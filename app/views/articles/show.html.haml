.tile.article
  .caption
    %h1= article.title
    %span.date= article.date
    %span.author= article.author
    %p= article.content

.comments
  .page-header
    %h2
      %span Comments
      - if user_signed_in?
        %span= link_to 'Add comment', new_comment_path(object: { type: object, id: article }), remote: true, class: 'btn btn-header add-comment'
  #comment-form
  - comments.each do |comment|
    - unless comment.created_at.nil?
      = render 'comments/comment', comment: comment