%h1 Newest news
- @news.each do |n|
  .news
    %h2= n.title
    %span= n.created_at.to_formatted_s(:db)
    %span= n.admin.email
    %p= n.content
%h1 Newest article
.article
  %h2= @article.title
  %span= @article.created_at.to_formatted_s(:db)
  %span= @article.admin.email
  %p= @article.content
